# Phase 17 — Admin dashboard KPIs and homepage collections

**Status:** Completed  
**Branch:** `develop`

## Goals

1. Homepage collections section from categories with product counts
2. Admin dashboard: pending offers count, pending returns, unverified high-price products
3. Contact / about static pages for trust (vintage stores need trust pages)

## Delivered

- Storefront `/about` and `/contact` (form → `contact_messages` table)
- Admin `/admin/messages` inbox with mark-read
- Footer links: about, contact, returns
- Dashboard operational KPIs: pending orders, offers, returns, authenticated products
- Homepage top-4 category collections linking to `shop?category=slug`

Push after each slice.
